---
phase: quick-260921-ga2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events/page.tsx
  - app/small-record/page.tsx
  - public/images/panic-room/ (untracked media, commit as-is)
  - public/videos/panic-crowd.mp4
  - public/videos/panic-lessovik.mp4
  - public/videos/panic-momal.mp4
  - public/videos/panic-letche.mp4
  - /Users/myltonvanbolderen/CLAUDE.md (outside repo, edit only, never commit)
autonomous: true
requirements: [EVT-01, EVT-02, EVT-03, EVT-04]

must_haves:
  truths:
    - "/events shows a section numbered 004 'Small Party' (Small Records × Panic Room · Paris 11 · September 2026) directly after Gambetta (003), preceded by a HorizontalRule"
    - "The section has a landscape crowd video hero, a 4-tile set-order strip (poster + Lessovik, Momal, Letché loops), a 6-photo grid and a wide crowd photo"
    - "A time table lists the 5 sets in order with genres, and a Venue/Date block shows Panic Room, 101 rue Amelot, Paris 11 / Friday, September 11, 2026, 21:00 — 05:00"
    - "The Coming soon section number reads 005"
    - "/small-record 'On the floor' teaser shows Panic Room as the first of three cards, all linking to /events"
    - "npx next build succeeds and out/events/index.html contains 'Panic Room' and 'panic-crowd.mp4'"
  artifacts:
    - path: "app/events/page.tsx"
      provides: "004 · Small Party × Panic Room section"
      contains: "SMALL PARTY × PANIC ROOM"
    - path: "app/small-record/page.tsx"
      provides: "Panic Room teaser card"
      contains: "/images/panic-room/trio.jpg"
  key_links:
    - from: "app/events/page.tsx"
      to: "public/videos/panic-*.mp4 + public/images/panic-room/*"
      via: "VideoLoop src/poster and next/image src"
      pattern: "/videos/panic-(crowd|lessovik|momal|letche)\\.mp4"
    - from: "app/small-record/page.tsx"
      to: "/events"
      via: "Link href in teaser cards map"
      pattern: "title: 'Panic Room'"
---

<objective>
Add the Small Party × Panic Room (Friday 11.09.26) recap as section 004 on /events, and add a Panic Room card as the first card of the /small-record "On the floor" teaser.

Purpose: a booker opening /events sees the crew's most recent night — its first under its own party name — at the same magazine quality as Gambetta.
Output: updated app/events/page.tsx and app/small-record/page.tsx, committed together with the already-prepared (untracked) Panic Room media. Do NOT deploy (no `vercel --prod`): the user reviews first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/Users/myltonvanbolderen/CLAUDE.md
@app/events/page.tsx
@app/small-record/page.tsx

<interfaces>
Already imported in app/events/page.tsx (no new imports needed):
- `Image` from 'next/image': always use `fill` + `className="object-cover"` + `unoptimized` inside a `relative ... overflow-hidden` sized parent.
- `AnimatedSection` (props used: blur, delay, direction, scale): wrap TEXT ONLY, never photos or videos (CLAUDE.md: causes flicker).
- `HorizontalRule color="bg-blanc/10"`.
- `VideoLoop({ src: string; poster?: string; className?: string })`: renders `<video autoPlay loop muted playsInline>`. Use `className="h-full w-full object-cover"` and put it in a parent with `bg-placeholder`.

Existing reference patterns in app/events/page.tsx (copy classes verbatim):
- Section 003 Gambetta = lines 182-367: header block (tag / h2 / meta / number), kicker `<p className="mb-10 font-display text-[clamp(1.2rem,3vw,1.9rem)] font-bold leading-[1.2] text-blanc/80 md:mb-12">` with terracotta second line, hero with bottom gradient caption, `mt-3 grid grid-cols-2 gap-3 md:grid-cols-3` gallery, `mt-12 grid gap-10 md:grid-cols-2 md:gap-16` two-column text, Venue/Date `grid grid-cols-2 gap-6 border-t border-blanc/10 pt-6`.
- Bottom label on a tile: `<div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-noir/90 to-transparent p-4"><span className="font-condensed text-[0.55rem] uppercase tracking-[0.3em] text-terracotta">…</span></div>`
- Timeline rows (Coming soon, lines 404-429): `border-t border-blanc/10` container, rows `flex items-center justify-between gap-4 border-b border-blanc/10 py-4`, index `font-condensed text-[0.55rem] tracking-[0.3em] text-terracotta/70`, name `font-display ... font-bold text-blanc/70`, right value `font-condensed text-[0.6rem] uppercase tracking-[0.3em] text-blanc/35`.

Media (already prepared, untracked — do not re-encode):
- public/images/panic-room/: affiche.jpg (787x1400 portrait poster), casae-prime.jpg, letche-casae.jpg, trio.jpg, crowd.jpg, faces.jpg, neon-sign.jpg, decks-blue.jpg, turntable.jpg (all landscape ~1920x1272), poster-panic-crowd.jpg, poster-panic-letche.jpg, poster-panic-momal.jpg, poster-panic-lessovik.jpg
- public/videos/: panic-crowd.mp4 (1280x720 LANDSCAPE), panic-lessovik.mp4, panic-momal.mp4, panic-letche.mp4 (portrait 9:16)
- public/asset/panic-room/ holds originals: gitignored, NEVER `git add` it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 004 · Small Party × Panic Room section on /events</name>
  <files>app/events/page.tsx</files>
  <action>
Insert a new section between the end of section 003 (the `</section>` closing Gambetta, ~line 367) and the `{/* ═══════ COMING SOON ═══════ */}` comment. Structure, top to bottom:

1. Comment `{/* ═══════ 004 · SMALL PARTY × PANIC ROOM ═══════ */}` then `<HorizontalRule color="bg-blanc/10" />`, then `<section className="px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-6xl">`.

2. Header in `<AnimatedSection blur>`, identical markup to Gambetta: tag "Past · Club Night", h2 "Small Party", meta "Small Records × Panic Room · Paris 11 · September 2026", right number "004".

3. Kicker in `<AnimatedSection delay={0.1}>` with the Gambetta kicker classes: "Our night. Our name on the door." `<br />` `<span className="text-terracotta">Five sets, one basement.</span>`.

4. Hero (NOT wrapped in AnimatedSection): `<div className="relative aspect-video w-full overflow-hidden bg-placeholder">` containing `<VideoLoop src="/videos/panic-crowd.mp4" poster="/images/panic-room/poster-panic-crowd.jpg" className="h-full w-full object-cover" />` and the bottom-gradient label pattern with `p-5 md:p-6` reading "The room, around 1am".

5. Set-order strip: `<div className="mt-3 grid grid-cols-2 gap-3 md:grid-cols-4">` with four `relative aspect-[9/16] w-full overflow-hidden` tiles:
   - affiche.jpg via `<Image fill className="object-cover" unoptimized alt="Small Party × Panic Room poster, September 11, 2026" />` (no label — the poster speaks for itself);
   - VideoLoop /videos/panic-lessovik.mp4, poster /images/panic-room/poster-panic-lessovik.jpg, label "Lessovik";
   - VideoLoop /videos/panic-momal.mp4, poster poster-panic-momal.jpg, label "Momal";
   - VideoLoop /videos/panic-letche.mp4, poster poster-panic-letche.jpg, label "Letché".
   Video tiles get `bg-placeholder` and the bottom-gradient label pattern (p-4).

6. Photo grid: `<div className="mt-3 grid grid-cols-2 gap-3 md:grid-cols-3">` of six `relative aspect-[3/2] w-full overflow-hidden` Image tiles in this order with these alts: casae-prime.jpg ("Casæ arms up at the decks with Lessovik"), neon-sign.jpg ("Panic Room red neon sign"), letche-casae.jpg ("Letché and Casæ behind the decks"), decks-blue.jpg ("DJ under blue light at Panic Room"), trio.jpg ("The crowd and the DJs at Panic Room"), turntable.jpg ("Panic Room slipmat on the turntable"). 6 tiles = 3 full rows on mobile and 2 on desktop, no orphans. No labels, no overlays.

7. Wide closer: `<div className="relative mt-3 aspect-[16/9] w-full overflow-hidden md:aspect-[21/9]">` with crowd.jpg (alt "The crowd smiling at the end of the night") + bottom-gradient label (p-5 md:p-6) "The crowd, 4:45am". Decision: faces.jpg is NOT used on the page (a 7th tile would orphan on the 3-col grid); it is still committed with the folder.

8. Two-column block `<div className="mt-12 grid gap-10 md:grid-cols-2 md:gap-16">`:
   - Left `<AnimatedSection delay={0.1}>`: paragraph with Gambetta paragraph classes: "Our first night under our own name. Panic Room opened its basement to Small Party: free entry, 9pm to 5am, five sets back to back. From Lessovik's vinyl house opening to Letché's latin-tech closing, through a Letché × Casæ tech-house B2B, Casæ's UK garage into techno and Momal's trance, the room never emptied." Keep "Panic Room" as PLAIN TEXT (the Instagram handle is not verified — do not link it). Then `<p className="mt-6 font-display text-[1.2rem] font-bold leading-[1.4] text-blanc/80">Thank you to everyone who came. We&apos;ll do it again soon.</p>`. Escape apostrophes with `&apos;` (react/no-unescaped-entities breaks the build otherwise).
   - Right `<AnimatedSection delay={0.2}>` with `<div className="space-y-8">`:
     a) "Time table" label (`font-condensed text-[0.55rem] uppercase tracking-[0.4em] text-terracotta`), then `<div className="mt-3 border-t border-blanc/10">` mapping a const array (define it inline in the JSX map, or as a module-level `const panicTimetable` above the component) in set order:
        `{ name: 'Lessovik', genre: 'House · Vinyl / CDJ' }`, `{ name: 'Letché b2b Casæ', genre: 'Tech House' }`, `{ name: 'Casæ', genre: 'UK Garage · Techno' }`, `{ name: 'Momal', genre: 'Trance' }`, `{ name: 'Letché', genre: 'Latin Tech · Bounce' }`.
        Each row (key = `${i}-${name}`, so keys stay unique even though Letché plays twice): `flex items-baseline justify-between gap-4 border-b border-blanc/10 py-3` with left `flex items-baseline gap-4` = index `String(i + 1).padStart(2, '0')` (terracotta/70 index class) + name `font-display text-[1rem] font-bold leading-none text-blanc/70 md:text-[1.15rem]`, right genre `shrink-0 text-right font-condensed text-[0.55rem] uppercase tracking-[0.25em] text-blanc/35`. Do NOT wrap each row in its own AnimatedSection (the column is already animated).
     b) Venue/Date grid copied from Gambetta: Venue = "Panic Room" / "101 rue Amelot" / "Paris 11"; Date = "Friday" / "September 11, 2026" / "21:00 — 05:00".

9. Close divs/section. In the Coming soon header, change the number "004" to "005".

Constraints (CLAUDE.md): no AnimatedSection around any photo/video, no full-bleed `bg-noir/XX` overlays on photos (bottom gradients only), next/image with `fill` + `unoptimized`, no new imports or components. Site copy stays in English.
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && grep -c "panic-room/\|videos/panic-" app/events/page.tsx && grep -n "^ *005$" app/events/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Section 004 exists after Gambetta with hero video, 4-tile strip, 6-photo grid, wide crowd photo, text + time table (5 rows in set order) + Venue/Date; Coming soon shows 005; TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Panic Room teaser card on /small-record + CLAUDE.md notes</name>
  <files>app/small-record/page.tsx, /Users/myltonvanbolderen/CLAUDE.md</files>
  <action>
In app/small-record/page.tsx "On the floor" teaser (~lines 260-305):
- Change comment `{/* Two compact past-event cards */}` to `{/* Three compact past-event cards (most recent first) */}`.
- Change grid classes `md:grid-cols-2` to `md:grid-cols-3` (keep `grid-cols-1 gap-4 md:gap-5`).
- Prepend as FIRST array item: `{ img: '/images/panic-room/trio.jpg', tag: 'Club Night', title: 'Panic Room', meta: 'Small Party · Paris 11 · Sep 2026' }`. Gambetta and Fête de la Musique follow unchanged; the auto index becomes 01/02/03. Nothing else changes (card markup, links to /events).

In /Users/myltonvanbolderen/CLAUDE.md (OUTSIDE the repo — edit it, never git add it), minimal edits:
- Line ~176 architecture tree: `events/ ... : Fete Musique, mix YouTube, Gambetta, Coming soon timeline` -> `: Fete Musique, mix YouTube, Gambetta, Small Party × Panic Room (11.09.26), Coming soon timeline`.
- Under `images/` after the `early-reflection/` line add `      panic-room/       # Photos + affiche + posters Small Party × Panic Room`; on the `videos/` line add `+ Panic Room (panic-crowd/lessovik/momal/letche)`.
- "Etat actuel" section: add one bullet `- **Dernier event** : Small Party × Panic Room (11.09.26) ajoute sur /events (section 004) et teaser /small-record`.
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && grep -n "title: 'Panic Room'\|md:grid-cols-3" app/small-record/page.tsx && grep -n "panic-room\|Panic Room" /Users/myltonvanbolderen/CLAUDE.md</automated>
  </verify>
  <done>Teaser renders 3 cards with Panic Room first; CLAUDE.md mentions Panic Room in Etat actuel, events/ line and images/videos tree.</done>
</task>

<task type="auto">
  <name>Task 3: Static build check and commit (code + media, no deploy)</name>
  <files>app/events/page.tsx, app/small-record/page.tsx, public/images/panic-room/, public/videos/panic-*.mp4</files>
  <action>
1. Run `npx next build` from the repo root (static export to out/). Fix any lint/type error (typically unescaped apostrophes or duplicate keys) in the two page files and rebuild until green.
2. Check the output: `grep -c "Panic Room" out/events/index.html` > 0 and `grep -c "panic-crowd.mp4" out/events/index.html` > 0; `grep -c "panic-room/trio.jpg" out/small-record/index.html` > 0.
3. Commit ONLY: `git add app/events/page.tsx app/small-record/page.tsx public/images/panic-room public/videos/panic-crowd.mp4 public/videos/panic-lessovik.mp4 public/videos/panic-momal.mp4 public/videos/panic-letche.mp4`. Do NOT add public/asset/, out/, .planning/config.json (pre-existing unrelated modification) or /Users/myltonvanbolderen/CLAUDE.md. Confirm with `git status --short` before committing that nothing under public/asset is staged.
   Message:
   ```
   feat(events): add Small Party × Panic Room recap (11.09.26)

   New section 004 on /events (crowd video hero, set-order loops, film photos,
   time table, venue/date) and Panic Room card first in /small-record teaser.

   Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
   ```
4. Do NOT run `vercel --prod` — the user reviews locally first (`npx next dev --turbopack --port 3000`).
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx next build && grep -c "Panic Room" out/events/index.html && grep -c "panic-crowd.mp4" out/events/index.html && grep -c "panic-room/trio.jpg" out/small-record/index.html && git log -1 --stat | grep -c "panic"</automated>
  </verify>
  <done>Build passes, built HTML contains the new section and teaser card, one commit includes both pages + Panic Room media and nothing from public/asset; nothing deployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static SSG content, no user input, no new external links (Panic Room kept as plain text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ga2-01 | I | git commit of media | mitigate | Stage explicit paths only; never add public/asset/ (originals, 136MB+) — checked via git status before commit |
| T-ga2-02 | T | external link target | accept | No new outbound link added (unverified Instagram handle left as plain text) |
</threat_model>

<verification>
- `npx next build` green (static export).
- out/events/index.html contains "Panic Room" and "panic-crowd.mp4"; out/small-record/index.html contains "panic-room/trio.jpg".
- Section order on /events: 001 Fête, 002 Mix, 003 Gambetta, 004 Small Party, 005 Coming soon.
- No AnimatedSection wraps an Image or VideoLoop in the new section.
</verification>

<success_criteria>
EVT-01..EVT-04 satisfied: Panic Room section with videos, photos and poster (EVT-01), 5-row time table with genres (EVT-02), venue + date/time block (EVT-03), Panic Room first card in /small-record teaser linking to /events (EVT-04). Committed, not deployed.
</success_criteria>

<output>
After completion, create `.planning/quick/260921-ga2-add-small-party-panic-room-recap-to-even/260921-ga2-SUMMARY.md`
</output>
