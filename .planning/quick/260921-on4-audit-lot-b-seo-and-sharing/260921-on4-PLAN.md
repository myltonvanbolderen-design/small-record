---
phase: quick-260921-on4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/seo.ts
  - app/layout.tsx
  - app/page.tsx
  - app/casae/page.tsx
  - app/letche/page.tsx
  - app/small-record/page.tsx
  - app/events/page.tsx
  - app/sitemap.ts
  - app/robots.ts
  - components/layout/Footer.tsx
  - public/og/og-casae.jpg
  - public/og/og-letche.jpg
  - public/og/og-label.jpg
  - public/og/og-events.jpg
autonomous: true
requirements: [PHASE-5-SEO-SHARING]

must_haves:
  truths:
    - "Each of the 5 public pages outputs its own og:title, og:description, og:url, og:image and a canonical link with trailing slash"
    - "https://small-records.com/sitemap.xml lists exactly the 5 public URLs; /robots.txt allows all and points to the sitemap"
    - "The Footer contains crawlable internal links to /, /small-record/, /casae/, /letche/, /events/"
    - "All booking emails (casae@, letche@, contact@) are mailto links"
    - "<html lang=\"en\"> and og:locale en_US"
    - "Layout emits an enriched MusicGroup JSON-LD; /events emits a MusicEvent JSON-LD array (Panic Room + Gambetta)"
  artifacts:
    - path: "lib/seo.ts"
      provides: "pageMetadata() helper building full openGraph + twitter + canonical"
      exports: ["pageMetadata", "SITE_URL"]
    - path: "app/sitemap.ts"
      provides: "Static sitemap (explicit list, force-static)"
      contains: "force-static"
    - path: "app/robots.ts"
      provides: "Static robots.txt"
      contains: "force-static"
    - path: "public/og/og-casae.jpg"
      provides: "1200x630 share image"
  key_links:
    - from: "app/*/page.tsx"
      to: "lib/seo.ts"
      via: "export const metadata = pageMetadata({...})"
      pattern: "pageMetadata\\("
    - from: "components/layout/Footer.tsx"
      to: "/small-record/"
      via: "next/link internal nav row"
      pattern: "/small-record/"
---

<objective>
Audit lot B (roadmap Phase 5 — SEO & partage): give every public page its own share preview + canonical, publish sitemap.xml/robots.txt, make /small-record/ crawlable via a Footer nav, turn emails into mailto links, switch language to English, and ship MusicGroup/MusicEvent JSON-LD.

Purpose: Links shared on Instagram/WhatsApp/iMessage show the right page (title, image, URL) and Google can discover and understand every page and the events.
Output: lib/seo.ts, app/sitemap.ts, app/robots.ts, public/og/*.jpg, updated layout/pages/Footer. 3 atomic commits. No deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@/Users/myltonvanbolderen/CLAUDE.md
@app/layout.tsx
@next.config.ts
@components/layout/Footer.tsx

<interfaces>
Current state (extracted, verified):

- next.config.ts: `output: 'export'`, `trailingSlash: true`, `images.unoptimized: true`.
- app/layout.tsx: metadataBase `https://small-records.com`; title `{ default: 'Small Records', template: '%s | Small Records' }`; openGraph `{ title, description, url: 'https://small-records.com', siteName, images: [{ url: '/og-image.jpg', width: 1200, height: 800 }], locale: 'fr_FR', type: 'website' }`; twitter summary_large_image; `<html lang="fr">`; inline MusicGroup JSON-LD `<script type="application/ld+json" dangerouslySetInnerHTML=...>` in <body> (members url = SoundCloud; sameAs = instagram, 2 soundclouds, linktree).
- Page metadata blocks (each `export const metadata: Metadata = { title, description }` right after imports):
  - app/page.tsx:14 title 'Small Records'
  - app/casae/page.tsx:11 title 'Casae'  — hero image `/images/casae/img_5547.jpg` (1920x1280)
  - app/letche/page.tsx:10 title 'Letche' — hero image `/images/letech/img_5611.jpg` (1920x1280; folder is spelled "letech")
  - app/small-record/page.tsx:13 title 'The Label' — hero image `/images/duo/img_3179.jpg` (1920x1080)
  - app/events/page.tsx:14 title 'Events' — Panic Room image `/images/panic-room/trio.jpg` (1920x1272, tracked in git)
- Plain-text emails to wrap:
  - app/casae/page.tsx ~L143-145: `<p className="font-display text-[clamp(1.2rem,3vw,2rem)] font-bold">casae@small-records.com</p>`
  - app/letche/page.tsx ~L178-180: same markup, letche@small-records.com
  - app/small-record/page.tsx ~L469-471: same markup, contact@small-records.com
  - Reference style (app/events/page.tsx ~L735): `<a href="mailto:contact@small-records.com" className="font-display text-[1.1rem] font-bold transition-colors hover:text-terracotta">`
- Header nav hrefs have no trailing slash and render only when `menuOpen` (not crawlable) — do NOT change Header.
- Footer link style: `font-condensed text-[0.6rem] uppercase tracking-[0.3em] text-blanc/30 transition-colors hover:text-terracotta`.
- app/v2 and app/carousel exist locally (excluded from deploy by .vercelignore) — never list them in the sitemap.
- Tools available: /opt/homebrew/bin/ffmpeg, /usr/bin/sips.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-page share images + metadata helper + page metadata, lang en</name>
  <files>public/og/og-casae.jpg, public/og/og-letche.jpg, public/og/og-label.jpg, public/og/og-events.jpg, lib/seo.ts, app/layout.tsx, app/page.tsx, app/casae/page.tsx, app/letche/page.tsx, app/small-record/page.tsx, app/events/page.tsx</files>
  <action>
1. Share images (finding 8): `mkdir -p public/og`, then for each source run
   `ffmpeg -y -loglevel error -i SRC -vf "scale=1200:-2,crop=1200:630" -q:v 3 -frames:v 1 public/og/NAME.jpg`
   (crop without x/y = centered; -q:v 3 ≈ quality 82; no text overlay):
   - og-casae ← public/images/casae/img_5547.jpg
   - og-letche ← public/images/letech/img_5611.jpg
   - og-label ← public/images/duo/img_3179.jpg
   - og-events ← public/images/panic-room/trio.jpg
   Verify each with `sips -g pixelWidth -g pixelHeight` = 1200x630. Home keeps /og-image.jpg (1200x800).

2. Create `lib/seo.ts`:
   ```ts
   import type { Metadata } from 'next'
   export const SITE_URL = 'https://small-records.com'
   type OgImage = { url: string; width: number; height: number; alt: string }
   export function pageMetadata(opts: { title: string | { absolute: string }; ogTitle: string; description: string; path: string; image: OgImage }): Metadata
   ```
   Returns `{ title, description, alternates: { canonical: path }, openGraph: { title: ogTitle, description, url: path, siteName: 'Small Records', locale: 'en_US', type: 'website', images: [image] }, twitter: { card: 'summary_large_image', title: ogTitle, description, images: [image.url] } }`. Relative paths resolve against layout metadataBase. Full openGraph per page is required because Next does NOT deep-merge openGraph (finding 1). `path` always has trailing slash ('/' for home) to match `trailingSlash: true` (finding 2).

3. Replace each page's `metadata` with `pageMetadata({...})` (keep `import type { Metadata }` only if still used; otherwise remove it to avoid lint noise). ogTitle = the full visible title (e.g. 'Casae | Small Records'). Descriptions (finding 9 — English, 140–160 chars; the executor must re-check length with node and adjust wording if outside 140–160):
   - Home: title `{ absolute: 'Small Records — DJ crew & label, Paris' }`, ogTitle same, path '/', image `{ url: '/og-image.jpg', width: 1200, height: 800, alt: 'DJ Casae and DJ Letche of Small Records' }`, description "Small Records is a Paris DJ crew and independent label: DJ Casae & DJ Letche. House, techno, baile funk, afrohouse, disco. Open for club and festival booking."
   - Casae: title 'Casae', ogTitle 'Casae | Small Records', path '/casae/', image /og/og-casae.jpg 1200x630 alt 'DJ Casae', description "DJ Casae, co-founder of Small Records in Paris. House, disco, grooves and afrohouse sets for clubs, festivals and private events. Mixes, photos and booking."
   - Letche: title 'Letche', path '/letche/', /og/og-letche.jpg, alt 'DJ Letche', description "DJ Letche, co-founder of Small Records in Paris. Techno, breaks, baile funk and ambient for clubs, festivals and raves. SoundCloud mixes, photos and booking."
   - The Label: title 'The Label', path '/small-record/', /og/og-label.jpg, alt 'Casae and Letche, Small Records', description "The Label: Small Records is an independent Paris music label and DJ crew founded by Casae & Letche. House, techno, baile funk, afrohouse. Booking and press."
   - Events: title 'Events', path '/events/', /og/og-events.jpg, alt 'Letché, Casæ and Lessovik at Panic Room', description "Small Records live in Paris: Small Party at Panic Room, Gambetta Club with Early Reflections, Fête de la Musique. Club nights, recaps, videos and booking." Do NOT change the visible "Small Party" h2.

4. app/layout.tsx (finding 5): `lang="fr"` → `lang="en"`; layout openGraph stays as fallback (404 etc.) but set `url: '/'`, `locale: 'en_US'`, and reuse the home description. Do NOT add alternates/canonical in layout (would leak to 404 and every page).
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx tsc --noEmit && for f in public/og/*.jpg; do sips -g pixelWidth -g pixelHeight $f | tail -2 | tr '\n' ' '; echo; done && for p in / /casae/ /letche/ /small-record/ /events/; do echo "== $p"; curl -s http://localhost:3000$p | grep -oE '<html lang="[a-z]+"|rel="canonical" href="[^"]+"|property="og:(url|title|image|locale)" content="[^"]+"'; done</automated>
  </verify>
  <done>4 JPEGs at 1200x630 in public/og/; tsc clean; each page returns lang="en", its own canonical (https://small-records.com/{path}/ with trailing slash), og:url, og:title, og:image, og:locale en_US; home title is "Small Records — DJ crew & label, Paris" (not doubled). Commit: `feat(seo): per-page share previews, canonicals and English locale` staging exactly lib/seo.ts, app/layout.tsx, the 5 page files, public/og/*.jpg.</done>
</task>

<task type="auto">
  <name>Task 2: sitemap.xml + robots.txt, Footer internal nav, mailto emails</name>
  <files>app/sitemap.ts, app/robots.ts, components/layout/Footer.tsx, app/casae/page.tsx, app/letche/page.tsx, app/small-record/page.tsx</files>
  <action>
1. `app/sitemap.ts` (finding 3): `export const dynamic = 'force-static'`; default export `sitemap(): MetadataRoute.Sitemap` from an EXPLICIT array of paths `['/', '/small-record/', '/casae/', '/letche/', '/events/']` mapped to `{ url: SITE_URL + path, lastModified: new Date(), changeFrequency: 'monthly', priority: path === '/' ? 1 : 0.8 }` (import SITE_URL from '@/lib/seo'). Never auto-discover routes; never include /v2 or /carousel.
2. `app/robots.ts`: `export const dynamic = 'force-static'`; return `{ rules: { userAgent: '*', allow: '/' }, sitemap: SITE_URL + '/sitemap.xml' }`.
3. Footer (finding 4): import `Link` from 'next/link'; add a `<nav aria-label="Footer">` row (flex-wrap, justify-center, gap-x-6 gap-y-3) placed between the wordmark and the "Small Records · Paris · 2025" line, with Links Home '/', The Label '/small-record/', Casae '/casae/', Letche '/letche/', Events '/events/' (trailing slashes). Class: `font-condensed text-[0.6rem] uppercase tracking-[0.3em] text-blanc/50 transition-colors hover:text-terracotta`. Also raise the existing 3 external links from `text-blanc/30` to `text-blanc/50`. Leave the unused `Image` import alone unless lint flags it (it is already there; removing it is fine). No other visual changes.
4. Emails (finding 6): in app/casae/page.tsx, app/letche/page.tsx, app/small-record/page.tsx replace the `<p className="font-display text-[clamp(1.2rem,3vw,2rem)] font-bold">X@small-records.com</p>` with `<a href="mailto:X@small-records.com" className="font-display text-[clamp(1.2rem,3vw,2rem)] font-bold transition-colors hover:text-terracotta">X@small-records.com</a>`. If the parent is a flex-col container the anchor lays out the same; if not, add `block` so it stays on its own line. Do not touch app/carousel.
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx tsc --noEmit && curl -s http://localhost:3000/sitemap.xml && curl -s http://localhost:3000/robots.txt && curl -s http://localhost:3000/small-record/ | grep -oE 'href="/(small-record|casae|letche|events)/?"' | sort -u && for p in casae letche small-record; do curl -s http://localhost:3000/$p/ | grep -oE 'href="mailto:[^"]+"' | sort -u; done && ! grep -rn "blanc/30" components/layout/Footer.tsx</automated>
  </verify>
  <done>sitemap.xml lists exactly 5 URLs (trailing slash, no /v2 or /carousel); robots.txt has `Allow: /` and `Sitemap: https://small-records.com/sitemap.xml`; every page's HTML contains footer links to all 5 routes; casae@/letche@/contact@ are mailto links. Commit: `feat(seo): sitemap, robots, footer nav and mailto links` staging exactly the 6 files.</done>
</task>

<task type="auto">
  <name>Task 3: JSON-LD MusicGroup enrichment + MusicEvent on /events, final build check</name>
  <files>app/layout.tsx, app/events/page.tsx</files>
  <action>
1. app/layout.tsx MusicGroup (finding 7): keep existing fields; set `url: 'https://small-records.com/'`, add `logo: 'https://small-records.com/images/logo/logo-wordmark-black.png'`, `image: 'https://small-records.com/og-image.jpg'`, `email: 'contact@small-records.com'`; members → `{ '@type': 'Person', name: 'Casae', url: 'https://small-records.com/casae/', sameAs: ['https://soundcloud.com/casae'] }` and `{ '@type': 'Person', name: 'Letche', url: 'https://small-records.com/letche/', sameAs: ['https://soundcloud.com/letchetony'] }`; append `'https://www.youtube.com/@SmallRecords_Music'` to group sameAs.
2. app/events/page.tsx: define `const eventsJsonLd = [...]` at module level and render `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(eventsJsonLd) }} />` as the first child inside the page's returned root element (server component, no "use client" change). Each item has `'@context': 'https://schema.org', '@type': 'MusicEvent'`, `eventStatus: 'https://schema.org/EventScheduled'`, `eventAttendanceMode: 'https://schema.org/OfflineEventAttendanceMode'`, `url: 'https://small-records.com/events/'`:
   - Panic Room: name 'Small Party — Small Records × Panic Room', startDate '2026-09-11T22:00:00+02:00', endDate '2026-09-12T05:00:00+02:00', isAccessibleForFree: true, location `{ '@type': 'Place', name: 'Panic Room', address: { '@type': 'PostalAddress', streetAddress: '101 rue Amelot', postalCode: '75011', addressLocality: 'Paris', addressCountry: 'FR' } }`, performer Persons: Lessovik, Casæ, Momal, Letché; organizer `{ '@type': 'Organization', name: 'Small Records', url: 'https://small-records.com' }`; image 'https://small-records.com/images/panic-room/trio.jpg'.
   - Gambetta: name 'Early Reflections × Small Records — Gambetta Club', startDate '2026-04-30T22:00:00+02:00', endDate '2026-05-01T06:00:00+02:00', location Place 'Le Gambetta Club' / '104 rue de Bagnolet', '75020', 'Paris', 'FR'; performer Persons: Letche, Casæ, DDG, Zeko, Copac, Scott Civil, Momal; organizer array `[{ '@type': 'Organization', name: 'Early Reflections' }, { '@type': 'Organization', name: 'Small Records', url: 'https://small-records.com' }]`; image 'https://small-records.com/images/early-reflection/packed-room.jpg'.
3. Final check: `npx tsc --noEmit`, curl JSON-LD from dev server, then `npx next build` (does not touch the running dev server's port; do not kill it). Confirm out/sitemap.xml and out/robots.txt exist and `grep -E 'v2|carousel'` on them returns nothing; confirm out/events/index.html contains MusicEvent. Do NOT deploy.
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx tsc --noEmit && curl -s http://localhost:3000/events/ | grep -o '"@type":"MusicEvent"' | wc -l && curl -s http://localhost:3000/ | grep -o 'youtube.com/@SmallRecords_Music' | head -1 && npx next build && test -f out/sitemap.xml && test -f out/robots.txt && ! grep -E 'v2|carousel' out/sitemap.xml out/robots.txt && grep -c MusicEvent out/events/index.html</automated>
  </verify>
  <done>/events HTML has 2 MusicEvent objects with valid ISO dates, addresses and performers; layout JSON-LD has logo, image, email, YouTube sameAs, member URLs on small-records.com; `next build` succeeds; out/sitemap.xml + out/robots.txt exist without /v2 or /carousel. Commit: `feat(seo): enrich MusicGroup JSON-LD and add MusicEvent schema on events` staging exactly app/layout.tsx and app/events/page.tsx (do not stage out/).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static build → public CDN | All output is public static HTML; no user input crosses into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-on4-01 | Information disclosure | app/sitemap.ts | mitigate | Explicit 5-path list; build check greps out/sitemap.xml for v2/carousel so internal tools are never advertised |
| T-on4-02 | Tampering (XSS) | JSON-LD dangerouslySetInnerHTML | accept | Content is hardcoded constants serialized with JSON.stringify, no user input |
| T-on4-03 | Information disclosure | mailto links | accept | Emails were already published in plain text; making them clickable adds no new exposure |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- curl on localhost:3000 for all 5 pages: distinct og:url/og:title/og:image, canonical with trailing slash, lang="en", og:locale en_US
- /sitemap.xml (5 URLs) and /robots.txt served
- Footer links to all 5 routes present on /small-record/
- `npx next build` succeeds; out/sitemap.xml and out/robots.txt without v2/carousel
- Dev server on port 3000 left running; no npm install; no deploy
</verification>

<success_criteria>
Roadmap Phase 5 criteria met: (1) per-page og:title/url/description + canonical; (2) sitemap.xml and robots.txt generated; (3) /small-record/ linked in footer, emails mailto everywhere, lang="en"; (4) MusicEvent JSON-LD for Panic Room and Gambetta. Three atomic commits ending with `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. Out of scope: www→apex redirect (Vercel setting), ImprovMX checks.
</success_criteria>

<output>
After completion, create `.planning/quick/260921-on4-audit-lot-b-seo-and-sharing/260921-on4-SUMMARY.md`
</output>
