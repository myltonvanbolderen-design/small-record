---
phase: quick-260921-qp5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/providers.tsx
  - app/page.tsx
  - app/small-record/page.tsx
  - app/casae/page.tsx
  - app/letche/page.tsx
  - app/events/page.tsx
  - app/not-found.tsx
  - components/layout/Header.tsx
  - components/layout/Footer.tsx
  - components/magazine/Marquee.tsx
  - components/magazine/FlipImage.tsx
  - components/magazine/PhotoSlider.tsx
  - components/magazine/VideoLoop.tsx
  - components/magazine/SoundCloudEmbed.tsx
  - components/animation/CountUp.tsx
autonomous: true
requirements: [PHASE-7]

must_haves:
  truths:
    - "axe-core reports 0 color-contrast violations on /, /small-record/, /casae/, /letche/, /events/ at 1440x900 and iPhone 13 (only documented ScrollRevealText mid-reveal false positives allowed)"
    - "axe-core reports 0 scrollable-region-focusable violations (PhotoSlider on /casae is focusable, named region)"
    - "Small red labels render in #E0525E (text-terracotta-light); large display red (h1/h2 '.', big numbers, display quotes) stays #CC2936"
    - "Keyboard: Tab reaches Menu, Enter opens it, focus moves to first menu link, aria-expanded=true, page content is inert, Escape closes and focus returns to the toggle"
    - "Skip link is visible on focus and jumps to the page's own <main id=main-content>"
    - "Every focusable element shows a 2px blanc outline on :focus-visible"
    - "With prefers-reduced-motion: reduce, Marquee is static, FlipImage stays on first image, CountUp shows final value (711) immediately, VideoLoop videos do not play (poster shown), motion transforms are disabled via MotionConfig"
    - "Screen readers read CountUp's final value only, not intermediate numbers"
  artifacts:
    - path: "app/providers.tsx"
      provides: "Client MotionConfig reducedMotion=user wrapper"
      contains: "reducedMotion=\"user\""
    - path: "app/globals.css"
      provides: "terracotta-light token + global :focus-visible"
      contains: "--color-terracotta-light: #E0525E"
    - path: "components/layout/Header.tsx"
      provides: "Accessible overlay menu"
      contains: "aria-expanded"
  key_links:
    - from: "app/layout.tsx"
      to: "app/providers.tsx"
      via: "<Providers> wraps Header + #main"
      pattern: "<Providers>"
    - from: "app/layout.tsx skip link"
      to: "<main id=\"main-content\"> in each public page"
      via: "href=#main-content"
      pattern: "main-content"
    - from: "components/layout/Header.tsx"
      to: "div#main in app/layout.tsx"
      via: "document.getElementById('main').inert = menuOpen"
      pattern: "getElementById\\('main'\\)"
---

<objective>
Audit lot D (roadmap Phase 7): bring the 5 public pages to WCAG 2.2 AA on axe-core, with a keyboard-operable menu, visible focus, a working skip link, and prefers-reduced-motion honored by motion and the looping components.

Purpose: bookers and promoters use all kinds of devices and settings. 229 contrast failures, a menu that doesn't work with a keyboard, and animations that ignore reduced motion all hurt the "magazine cover" impression and legal accessibility.
Output: code changes in 3 atomic commits + before/after axe report + keyboard/reduced-motion test results + screenshots in scratchpad/lotd/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@/Users/myltonvanbolderen/CLAUDE.md
@app/globals.css
@app/layout.tsx
@components/layout/Header.tsx
@components/layout/Footer.tsx

## Scope
Public pages ONLY: app/page.tsx, app/small-record, app/casae, app/letche, app/events, app/not-found.tsx + shared components. Do NOT touch app/v2, app/carousel, components/v2.

## Hard rules (environment)
- SP = /private/tmp/claude-501/-Users-myltonvanbolderen-small-record/54b5f0cd-5a66-4186-b337-6738006c59de/scratchpad. Put all scripts/outputs in $SP/lotd/.
- The dev server on http://localhost:3000 runs in the user's terminal: NEVER kill or restart it, NEVER run npm install. If it's not reachable, run a second dev server from the isolated copy ($SP/buildcheck, see below) on port 3100 in the background, and stop it when you're done.
- Any `next build` ONLY in the isolated copy: `rsync -a --delete --exclude .next --exclude out --exclude public/asset --exclude node_modules /Users/myltonvanbolderen/small-record/ $SP/buildcheck/` then `ln -sfn /Users/myltonvanbolderen/small-record/node_modules $SP/buildcheck/node_modules`.
- Playwright: `createRequire(process.env.HOME + '/Downloads/PANIC ROOM/_kit/package.json')` then `require('playwright')` (chromium already installed). axe: the local `$SP/axe.min.js` (4.13) via `page.addScriptTag({ content })`, or https://cdn.jsdelivr.net/npm/axe-core@4.13/axe.min.js.
- Reuse/adapt $SP/axe.mjs (it currently targets production and scrolls 400px/120ms): change it to take BASE from argv (default http://localhost:3000), use viewports desktop 1440x900 and `devices['iPhone 13']`, scroll progressively by 300px every 60ms to the bottom, then wait 1.5s before axe.run, use `waitUntil: 'load'` (never networkidle: videos loop), write JSON to an output path from argv.
- Commits: stage exact paths only (never `git add -A`/`.`, `.claude/` is untracked and must stay so). Each commit message ends with a blank line then `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. No deploy (no `vercel`).

## Locked decisions (from user, do not reopen) — referenced as D-1..D-10 below
D-1 token `--color-terracotta-light: #E0525E` (5.22:1 on noir) for all SMALL red text; `text-terracotta` (#CC2936) kept for large display elements; border/bg terracotta unchanged; `hover:text-terracotta` on links kept.
D-2 informative small grey text below blanc/50 → at least `text-blanc/55`.
D-3 purely decorative text (folios "000"/"001"/"002"/"003", section-number spans, Marquee band) → `aria-hidden="true"`, keep faint styling.
D-4 menu: aria-expanded, aria-controls, no static aria-label (visible text is the name), Escape closes, focus first link on open / back to toggle on close, `inert` on #main while open. No body scroll lock exists today → don't add one.
D-5 `<MotionConfig reducedMotion="user">` in a client Providers in app/layout.tsx + `useReducedMotion` in Marquee, FlipImage, CountUp, VideoLoop.
D-6 no global pause button in this lot → documented as a remaining gap (WCAG 2.2.2) in SUMMARY.
D-7 global `:focus-visible` outline in globals.css; skip link visible on focus, targets `#main-content` on each public `<main>`.
D-8 PhotoSlider: tabIndex 0, role="region", aria-label prop (default "Photo gallery"; Casae: "Photos of Casae").
D-9 CountUp: final value exposed to AT, animated digits aria-hidden, SSR still renders the final value.
D-10 SoundCloud color `%23C4622D` → `%23CC2936`.

## Contrast reference (on #0A0A0A)
blanc/15 1.43, /20 1.69, /25 2.02, /30 2.42, /35 2.90, /40 3.46, /45 4.10, /50 4.83 (first ≥4.5), /55 5.64. terracotta #CC2936 3.71 (passes only as LARGE text: ≥24px, or ≥18.66px bold), terracotta/70 2.31, /60 1.97, /50 ~1.6. #E0525E 5.22.
Large text threshold is 3:1 → blanc/40 (3.46) is OK for large bold display text (e.g. app/page.tsx:411 clamp(1.2rem..) bold, menu display links 2rem+). Everything under ~18px needs ≥4.5:1.

<interfaces>
Current relevant code (extracted — no need to explore):

app/layout.tsx body (current):
```tsx
<a href="#main" className="sr-only focus:not-sr-only fixed left-4 top-4 z-[100] rounded bg-terracotta px-4 py-2 font-condensed text-sm text-blanc focus:outline-none">Skip to content</a>
<ScrollToTop />
<Header />
<div id="main">
  {children}
  <Footer />
</div>
```
`id="main"` is referenced only by the skip link (grep confirmed). Keep the wrapper and its id: Header will use it for `inert`.

Each public page's root: `<main className="min-h-screen bg-noir text-blanc">` at app/page.tsx:33, app/small-record/page.tsx:30, app/casae/page.tsx:28, app/letche/page.tsx:27, app/events/page.tsx:99; app/not-found.tsx:6 `<main className="relative flex min-h-screen ...">`.

Header.tsx: `'use client'`, `useState(menuOpen)`, toggle `<button onClick={() => setMenuOpen(!menuOpen)} aria-label="Menu">{menuOpen ? 'Close' : 'Menu'}</button>`, overlay `<AnimatePresence>{menuOpen && <motion.div className="fixed inset-0 z-40 ..." onClick={close}><nav onClick={stopPropagation}>` with "Sommaire" label (text-terracotta 0.6rem), nav links (num span text-terracotta/70 0.7rem; label display clamp(2rem,6vw,4.5rem) text-blanc/40 inactive), social links (0.6rem text-blanc/30). Center masthead `Small Records · Paris` 0.6rem text-blanc/30 (desktop only).

Footer.tsx: links 0.6rem text-blanc/50 (OK, 4.83), copyright `Small Records · Paris · 2025` text-blanc/25 → must be /55.

Marquee.tsx: `motion.div animate={{ x: [0, '-50%'] }}` infinite, text-blanc/20, content doubled.
FlipImage.tsx: setInterval cycling `index`, images toggled with `visibility`.
PhotoSlider.tsx: root `<div ref={containerRef} className="overflow-x-auto scrollbar-hide ...">` with draggable motion.div; props `{ photos, className }`.
VideoLoop.tsx: IntersectionObserver sets `video.src` lazily and play()/pause(); `preload="none"`, `poster`, `aria-hidden`. All 9 VideoLoop usages in events/page.tsx pass a poster.
CountUp.tsx: `useState(target)` (SSR final value), resets to 0 on client until in view, `animate(0, target, { onUpdate })`; renders `<span ref className style={{fontVariantNumeric:'tabular-nums'}}>{prefix}{current}{suffix}</span>`; non-matching values render plain `<span>{value}</span>`. Used at app/page.tsx:264 and app/events/page.tsx:347 (stats band, "711").
SoundCloudEmbed.tsx: src string contains `&color=%23C4622D`.
motion 12.38 exports `MotionConfig`, `useReducedMotion` from 'motion/react'. React 19.1 supports the boolean `inert` prop / `el.inert` DOM property.
</interfaces>

## Contrast inventory (grep of public pages, line numbers at plan time)
Small red labels (0.5–0.7rem) `text-terracotta` → `text-terracotta-light` (D-1): all kicker spans `font-condensed text-[0.55rem] ... text-terracotta` in app/page.tsx (108,155,181,206,229,251,319,352), small-record (81,151,248,302,343,380,404,420,444,474), casae (83,127,148), letche (84,162,183), events (133,165,199,214,226,238,250,325,382,451,485,500,521,542,583,638,664,689,705,719,751,784), not-found (19), Header "Sommaire".
Opacity variants on small red → `text-terracotta-light` (D-1): app/page.tsx:380 `/60`, small-record:181 `/50`, events:398 & 781 `/70`, Header num `/70`. These are list ordinal numbers ("01","02"...) that carry order info → keep readable, don't aria-hide.
KEEP `text-terracotta` (large): the "." spans inside h1/h2 (page.tsx:67, events:139, 755, not-found:25), events:185 & 471 (span inside clamp(1.2rem..1.9rem) bold display paragraph — large bold ≥18.66px at min 1.2rem=19.2px ✔), small-record:361 (display quote clamp 1.6rem), big numbers (page.tsx:269 i===0, events:353 4.5rem), all `hover:text-terracotta`, `decoration-terracotta/*`, `border-terracotta*`, `bg-terracotta`.
Grey small/body text → `text-blanc/55` (D-2): page.tsx 51,54 (/40 cover meta), 81 (/30 "Casae & Letche · Paris"), 235,325 (/40 subtitles), 272 (/45 stat labels), 280 (/40 link); small-record 58 (/40), 196 (/45 paragraph), 254,386,426,450,483 (/40), 260,324 (/40 links), 312 (/30 ordinal), 491,499 (/30 links); casae 44 (/40), 162,170 (/40 links); letche 44 (/40), 197,205 (/40 links); events 121,124 (/40), 171,457,644,670 (/40), 357 (/45), 405 (`text-blanc/45 ... md:text-blanc/35` → `text-blanc/55 ... md:text-blanc/55`), 414,426,601,613 (/30 "Venue"/"Date"), 791 (/35 `when`), 810 (/45), 826,834 (/40 links); Header social links (/30); Footer copyright (/25).
Leave as-is: app/page.tsx:411 (display bold ≥19.2px, /40 = 3.46 ≥ 3:1). If axe still flags it, raise to /55.
Decorative → add `aria-hidden="true"`, keep styling (D-3): folios app/page.tsx:85 ("001"), 133 ("002"), 162 ("003") + 329; small-record 139, 154, 390; casae 134; letche 169; events 148 ("000"), 175, 461, 648, 674, 758 (all `hidden ... text-blanc/20 md:block` section numbers and `text-blanc/15|/20` folios); Header center masthead "Small Records · Paris" (duplicates the logo alt → aria-hidden); Marquee root.
Before aria-hiding each, read the span: it must contain only a folio/section number. Line numbers may drift — match by content with grep, not by line.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline axe + keyboard, focus, skip link, accessible menu, PhotoSlider region</name>
  <files>app/globals.css, app/layout.tsx, app/page.tsx, app/small-record/page.tsx, app/casae/page.tsx, app/letche/page.tsx, app/events/page.tsx, app/not-found.tsx, components/layout/Header.tsx, components/magazine/PhotoSlider.tsx</files>
  <action>
0. BASELINE first (before any edit): `mkdir -p $SP/lotd`; copy $SP/axe.mjs to $SP/lotd/axe-local.mjs and adapt as described in context (argv BASE + OUT, 1440x900 + devices['iPhone 13'], 300px/60ms scroll, 1.5s wait, waitUntil 'load', same tags list). For each page+viewport store violations (id, count, up to 10 node targets + html snippet 120 chars + failureSummary). Also print a summary table: per page/viewport count of color-contrast nodes and scrollable-region-focusable nodes. Run against http://localhost:3000 → $SP/lotd/axe-before.json. Check `curl -sI http://localhost:3000/` first; if unreachable follow the port-3100 fallback from context.

1. app/globals.css (D-7, D-1): in `@theme` add `--color-terracotta-light: #E0525E;` right after `--color-terracotta` with a comment "small red text (5.22:1 on noir); #CC2936 only for large display". In `@layer base` add:
   `:focus-visible { outline: 2px solid var(--color-blanc); outline-offset: 3px; }`
   (do not add `:focus { outline: none }` anywhere).

2. app/layout.tsx (D-7): skip link → `href="#main-content"`, remove `focus:outline-none`, make it visible on focus: `sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[100] rounded bg-terracotta px-4 py-2 font-condensed text-sm text-blanc` (blanc on #CC2936 = ~4.7:1; the global focus-visible outline shows too). Keep `<div id="main">` wrapper (Header uses it for inert).

3. Add `id="main-content"` to the root `<main>` in app/page.tsx, app/small-record/page.tsx, app/casae/page.tsx, app/letche/page.tsx, app/events/page.tsx, app/not-found.tsx (D-7). Also `tabIndex={-1}` on those `<main>` so focus lands there reliably in Safari after activating the skip link, plus class `outline-none` ONLY on main (a tabIndex=-1 container doesn't need a ring; it's never reached with Tab).

4. components/layout/Header.tsx (D-4):
   - `const toggleRef = useRef<HTMLButtonElement>(null)`, `const menuRef = useRef<HTMLElement>(null)` (on the `<nav>`), `const wasOpen = useRef(false)`.
   - Toggle button: remove `aria-label="Menu"`; add `ref={toggleRef}`, `type="button"`, `aria-expanded={menuOpen}`, `aria-controls="site-menu"`. Visible text stays "Menu"/"Close" (it is the accessible name → satisfies 2.5.3).
   - Overlay `motion.div` gets `id="site-menu"`; the `<nav>` gets `ref={menuRef}` and `aria-label="Main"`.
   - `useEffect` on `[menuOpen]`: 
     - `const main = document.getElementById('main'); if (main) main.inert = menuOpen;`
     - if open: `wasOpen.current = true`; add `keydown` listener on `document`: `if (e.key === 'Escape') setMenuOpen(false)`; focus first link: `requestAnimationFrame(() => menuRef.current?.querySelector<HTMLElement>('a')?.focus())` (the nav is mounted in the same commit as menuOpen=true because AnimatePresence renders children immediately; rAF is enough — if the test shows focus not landing, use a 2x rAF or a `setTimeout(…, 0)`).
     - if closed and `wasOpen.current`: `wasOpen.current = false; toggleRef.current?.focus()`.
     - cleanup removes the keydown listener and sets `main.inert = false` on unmount.
   - Close the menu on route change too (already done via link onClick — keep).
   - Contrast (D-1/D-2/D-3) inside Header in this same task since the file is touched here: "Sommaire" `text-terracotta` → `text-terracotta-light`; nav num `text-terracotta/70` → `text-terracotta-light`; social links `text-blanc/30` → `text-blanc/55` (keep hover:text-terracotta); center masthead span → add `aria-hidden="true"` (decorative duplicate of logo name). Inactive nav display labels `text-blanc/40` stay (large text ≥2rem, 3.46:1 ≥ 3:1).
   - Logo `<Link href="/">` has an img alt "Small Records" → fine. Add `aria-label` nowhere else.

5. components/magazine/PhotoSlider.tsx (D-8): add prop `label?: string` (default `'Photo gallery'`); root div gets `tabIndex={0}`, `role="region"`, `aria-label={label}`. Arrow keys scroll natively once focused; no custom handler needed. app/casae/page.tsx: pass `label="Photos of Casae"`.

Commit: `fix(a11y): keyboard menu, focus-visible, skip link to main, focusable photo slider` with the exact paths above (+ Co-Authored-By line).
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx tsc --noEmit && test -s $SP/lotd/axe-before.json && grep -c 'id="main-content"' app/page.tsx app/small-record/page.tsx app/casae/page.tsx app/letche/page.tsx app/events/page.tsx app/not-found.tsx && grep -q 'aria-expanded' components/layout/Header.tsx && ! grep -q 'aria-label="Menu"' components/layout/Header.tsx</automated>
  </verify>
  <done>Baseline axe JSON exists with per-page counts; tsc clean; each of the 6 public mains has id="main-content"; skip link targets it and is visible on focus; menu has aria-expanded/aria-controls, Escape handler, focus management and inert on #main; PhotoSlider is a focusable named region. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Reduced motion (MotionConfig + Marquee/FlipImage/CountUp/VideoLoop) and CountUp SR text</name>
  <files>app/providers.tsx, app/layout.tsx, components/magazine/Marquee.tsx, components/magazine/FlipImage.tsx, components/animation/CountUp.tsx, components/magazine/VideoLoop.tsx</files>
  <action>
1. Create app/providers.tsx (D-5): `'use client'`; `import { MotionConfig } from 'motion/react'`; `export function Providers({ children }: { children: React.ReactNode }) { return <MotionConfig reducedMotion="user">{children}</MotionConfig> }`. In app/layout.tsx wrap `<ScrollToTop />`, `<Header />` and `<div id="main">…</div>` in `<Providers>` (skip link and JSON-LD script can stay outside). layout.tsx stays a Server Component (metadata export unchanged).

2. Marquee.tsx (D-5, D-3): `const reduce = useReducedMotion()`. Root div gets `aria-hidden="true"` (decorative band, text duplicates genres named elsewhere). If `reduce`: render a plain `<div className="inline-block …same classes…">{text}</div>` (single copy, no motion). Else current motion.div unchanged. Note `useReducedMotion()` returns false/null during SSR then updates on the client — acceptable (static first paint of the animated version is identical at x=0).

3. FlipImage.tsx (D-5): `const reduce = useReducedMotion()`; in the effect `if (reduce) { setIndex(0); return }` before creating the interval; add `reduce` to deps. First image stays visible.

4. CountUp.tsx (D-5, D-9):
   - `const reduce = useReducedMotion()`; in the effect: `if (!counts) return; if (reduce) { setCurrent(target); return }` BEFORE the `!isInView` reset (so reduced-motion users never see 0). Add `reduce` to deps.
   - Markup for the counting branch: `<span ref={ref} className={className} style={{ fontVariantNumeric: 'tabular-nums' }}><span className="sr-only">{value}</span><span aria-hidden="true">{prefix}{current}{suffix}</span></span>`. Do NOT put aria-label on the plain span (axe `aria-prohibited-attr` flags aria-label on generic elements). SSR still outputs the final value in both inner spans (initial state = target). Keep the non-counting branch as is.
   - Check visually that `sr-only` inside the stats number doesn't shift layout (it's absolutely positioned 1px — fine).

5. VideoLoop.tsx (D-5): `const reduce = useReducedMotion()`. In the effect: `if (reduce) { video.pause(); return }` right after `video.muted = true` (no src assigned → nothing downloaded, poster shown). Add `reduce` to deps `[src, reduce]`. If reduce flips to true after a video already loaded, the early `video.pause()` stops it. Keep `aria-hidden`, `preload="none"`, poster.

6. No change to ParallaxImage/ParallaxTitle/ScrollRevealText (style-bound scroll motion values are not in D-5 scope) — list them in SUMMARY as remaining motion under reduced-motion, together with D-6 (no global pause control, WCAG 2.2.2 gap).

Commit: `feat(a11y): honor prefers-reduced-motion in motion, marquee, flip, count-up, video loops` with exact paths (+ Co-Authored-By line).
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx tsc --noEmit && grep -q 'reducedMotion="user"' app/providers.tsx && grep -q '<Providers>' app/layout.tsx && grep -l useReducedMotion components/magazine/Marquee.tsx components/magazine/FlipImage.tsx components/animation/CountUp.tsx components/magazine/VideoLoop.tsx | wc -l | grep -q 4</automated>
  </verify>
  <done>tsc clean; Providers with MotionConfig reducedMotion="user" wraps the app; the 4 components branch on useReducedMotion; CountUp exposes only the final value to AT; SSR output still contains "711". Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Contrast sweep (terracotta-light, blanc/55, aria-hidden folios), SoundCloud color, full verification</name>
  <files>app/page.tsx, app/small-record/page.tsx, app/casae/page.tsx, app/letche/page.tsx, app/events/page.tsx, app/not-found.tsx, components/layout/Footer.tsx, components/magazine/SoundCloudEmbed.tsx</files>
  <action>
1. Apply the "Contrast inventory" from context, matching by content (lines may have drifted):
   - D-1: every small-label `text-terracotta` (font-condensed, text-[0.5rem]…[0.7rem]) → `text-terracotta-light`; `text-terracotta/50|/60|/70` on small ordinals → `text-terracotta-light`. Do NOT touch `hover:text-terracotta`, `decoration-terracotta*`, `border-terracotta*`, `bg-terracotta`, nor the large display uses listed under KEEP.
   - D-2: listed grey text → `text-blanc/55` (including the `md:text-blanc/35` variant on events genre column and Footer copyright `/25`).
   - D-3: `aria-hidden="true"` on folio / section-number spans listed (verify content is only a number like "000"…"005").
   - Sanity grep afterwards (public pages + components/layout + components/magazine): `grep -nE "text-blanc/(1|2|3|4)[05]\b|text-terracotta/[0-9]" …` → every remaining hit must be either aria-hidden, large display text, or non-text (border/bg). Also `grep -n "[^:-]text-terracotta\b"` → every remaining hit must be large display text.
2. SoundCloudEmbed.tsx (D-10): `%23C4622D` → `%23CC2936`.
3. `npx tsc --noEmit`.
4. Commit: `fix(a11y): AA contrast for small labels (terracotta-light, blanc/55), hide decorative folios, SoundCloud brand red` with exact paths (+ Co-Authored-By line).
5. VERIFICATION (scripts in $SP/lotd/, all against http://localhost:3000 or the 3100 fallback; wait for HMR — reload and confirm the new class is present in the served HTML first):
   a. axe after: `node $SP/lotd/axe-local.mjs http://localhost:3000 $SP/lotd/axe-after.json`. Target: 0 `color-contrast` nodes on all 5 pages × 2 viewports, 0 `scrollable-region-focusable`. If remaining contrast nodes are inside a ScrollRevealText span (opacity < 1 mid-reveal; check `el.closest` computed opacity in the node html/target), classify them as known false positives and list them. Any other remaining node → fix it (same rules) and re-run. Also confirm no NEW violation types vs before (e.g. aria-prohibited-attr, aria-valid-attr-value on aria-controls, nested-interactive). Print a before/after table (per page/viewport: color-contrast, scrollable-region-focusable, total violations).
   b. $SP/lotd/keyboard.mjs (desktop 1440x900, page /events/): press Tab until `document.activeElement` is the button with text "Menu" (max 10 tabs; note the first Tab should hit the skip link — assert it becomes visible: boundingClientRect width > 1); press Enter → assert `aria-expanded="true"`, activeElement is an `a` inside `#site-menu`, `document.getElementById('main').inert === true`; press Tab a few times → focus never goes into #main; press Escape → assert `aria-expanded="false"`, `#site-menu` gone (wait 600ms for exit anim), activeElement is the toggle, `#main.inert === false`. Also test skip link: fresh load, Tab once, Enter → `location.hash === '#main-content'` and activeElement id `main-content`. Print PASS/FAIL per assertion.
   c. $SP/lotd/reduced.mjs: `browser.newContext({ reducedMotion: 'reduce', viewport 1440x900 })`. On /casae/: read the Marquee inner element `getComputedStyle(el).transform` at t0 and t0+1s → identical. On /: scroll the stats band into view and immediately read the CountUp text → contains "711" (never 0); on /small-record/: FlipImage visible image src constant over 2s. On /events/: scroll through the page, then every `video` has `paused === true` and empty `currentSrc`. Print PASS/FAIL. Run the same /events/ video check with a normal context to confirm videos DO play there (no regression).
   d. Screenshots → $SP/lotd/: `events-desktop.png`, `events-mobile.png`, `home-desktop.png`, `home-mobile.png` (viewport shots of the Panic Room lineup section on /events/ and the kicker/stats area on /, plus fullPage versions). Open them with Read and check the red labels still read as brand red (#E0525E is a lighter tone of the same red) and nothing else changed.
   e. Optional build sanity in the isolated copy ($SP/buildcheck, rsync + symlink as in context): `npx next build` succeeds.
6. In the SUMMARY: before/after axe counts table, documented ScrollRevealText false positives (if any), keyboard + reduced-motion PASS results, screenshot paths, and remaining gaps: D-6 (no global pause control for autoplay loops, WCAG 2.2.2), parallax/scroll-linked transforms still run under reduced motion, `hover:text-terracotta` hover state on small links is 3.71:1 (kept by decision).
  </action>
  <verify>
    <automated>cd /Users/myltonvanbolderen/small-record && npx tsc --noEmit && grep -q '%23CC2936' components/magazine/SoundCloudEmbed.tsx && node $SP/lotd/axe-local.mjs http://localhost:3000 $SP/lotd/axe-after.json && node $SP/lotd/keyboard.mjs && node $SP/lotd/reduced.mjs</automated>
  </verify>
  <done>axe-after shows 0 color-contrast (except documented ScrollRevealText mid-reveal nodes) and 0 scrollable-region-focusable on 5 pages × desktop/iPhone 13; keyboard and reduced-motion scripts all PASS; screenshots saved and reviewed; SoundCloud uses %23CC2936; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Static site; changes are markup/CSS/client behavior only, no new input or network endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qp5-01 | D | Header inert on #main | mitigate | cleanup sets `main.inert = false` on close and unmount so the page can never stay locked |
| T-qp5-02 | T | SoundCloud iframe src | accept | only a constant color param changes; URL still built with encodeURIComponent |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after each task.
- axe before/after on localhost: 0 color-contrast (minus documented ScrollRevealText false positives), 0 scrollable-region-focusable, no new violation types.
- keyboard.mjs and reduced.mjs all PASS; normal context videos still play.
- Screenshots in $SP/lotd/ reviewed.
- Dev server on :3000 never restarted; no npm install; no deploy; 3 commits with exact paths.
</verification>

<success_criteria>
Roadmap Phase 7 criteria met: (1) 0 axe color-contrast on 5 pages desktop+mobile with small labels in #E0525E and #CC2936 kept for large elements; (2) menu has aria-expanded, Escape, managed focus, inert background; (3) prefers-reduced-motion honored by motion (MotionConfig), Marquee, FlipImage, CountUp, VideoLoop; (4) visible focus-visible everywhere and skip link to the real <main>.
</success_criteria>

<output>
After completion, create `.planning/quick/260921-qp5-audit-lot-d-accessibility/260921-qp5-SUMMARY.md`
</output>
